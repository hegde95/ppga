torch==2.3.1

# JAX
# Comment: JAX and PyTorch are difficult to install together due to CUDA
# dependencies; these versions seem to both allow CUDA 12.1 -- see here for more
# info:
# https://github.com/google/jax/issues/18032
jax[cuda12]==0.4.28
brax==0.9.4

jaxopt==0.8.3
flax==0.8.4
chex==0.1.86
gym==0.26.2
wandb
evotorch==0.5.1
colorlog

ribs[all]==0.7.1
qdax

python-box[all]~=7.0
